<h1> All the Gardens</h1>
	
	<li>
		<%=link_to "Add a new Garden", new_garden_path %>
	</li>

<ul>
	<% @gardens.each do|garden| %>
		
		<li>
			<%= link_to "Name: #{garden.name}, Location: #{garden.location}", garden_path(garden.id) %>
        </li>
        
        <li>
			<%= link_to "edit", edit_garden_path(garden.id) %>
	    </li>

	<% end%>
</ul>

